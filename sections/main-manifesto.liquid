{% comment %}
  TRUST.NO.OUTPUT — MANIFESTO Page Section

  A storytelling page for the cyberpunk streetwear brand featuring:
  - 4 distinct vertical sections with anchor IDs
  - Alternating text/image layouts for scrollytelling
  - Dark mode cyberpunk aesthetic
  - Editable content via Shopify schema
{% endcomment %}

{%- liquid
  assign origin_title = section.settings.origin_title | default: 'ORIGIN OF THE GLITCH'
  assign origin_text = section.settings.origin_text
  assign origin_image = section.settings.origin_image

  assign philosophy_title = section.settings.philosophy_title | default: 'RULES OF THE SIMULATION'
  assign philosophy_text = section.settings.philosophy_text
  assign philosophy_image = section.settings.philosophy_image

  assign design_title = section.settings.design_title | default: 'DESIGN DNA'
  assign design_text = section.settings.design_text
  assign design_image = section.settings.design_image

  assign ethics_title = section.settings.ethics_title | default: 'HUMAN LAYER'
  assign ethics_text = section.settings.ethics_text
  assign ethics_image = section.settings.ethics_image
-%}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<section class="tno-manifesto manifesto-trust section-{{ section.id }}-padding">
  <div class="manifesto__container page-width">
    {%- comment %} Section 1: ORIGIN (Text Left / Image Right) {%- endcomment %}
    <div class="tno-manifesto__section" id="origin">
      <div class="tno-manifesto__grid grid grid--1-col grid--2-col-desktop grid--2-col-tablet">
        <div class="tno-manifesto__content grid__item">
          <h2 class="tno-manifesto__title">{{ origin_title }}</h2>
          {%- if origin_text != blank -%}
            <div class="tno-manifesto__text">
              {{ origin_text }}
            </div>
          {%- endif -%}
        </div>
        <div class="tno-manifesto__media grid__item">
          {%- if origin_image != blank -%}
            {{
              origin_image
              | image_url: width: 1200
              | image_tag:
                loading: 'lazy',
                sizes: '(min-width: 990px) 50vw, 100vw',
                alt: origin_image.alt,
                class: 'tno-manifesto__image'
            }}
          {%- else -%}
            <div class="tno-manifesto__placeholder">
              {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>

    {%- comment %} Section 2: PHILOSOPHY (Image Left / Text Right) {%- endcomment %}
    <div class="tno-manifesto__section" id="philosophy">
      <div class="tno-manifesto__grid grid grid--1-col grid--2-col-desktop grid--2-col-tablet tno-manifesto__grid--reverse">
        <div class="tno-manifesto__media grid__item">
          {%- if philosophy_image != blank -%}
            {{
              philosophy_image
              | image_url: width: 1200
              | image_tag:
                loading: 'lazy',
                sizes: '(min-width: 990px) 50vw, 100vw',
                alt: philosophy_image.alt,
                class: 'tno-manifesto__image'
            }}
          {%- else -%}
            <div class="tno-manifesto__placeholder">
              {{ 'lifestyle-2' | placeholder_svg_tag: 'placeholder-svg' }}
            </div>
          {%- endif -%}
        </div>
        <div class="tno-manifesto__content grid__item">
          <h2 class="tno-manifesto__title">{{ philosophy_title }}</h2>
          {%- if philosophy_text != blank -%}
            <div class="tno-manifesto__text">
              {{ philosophy_text }}
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>

    {%- comment %} Section 3: DESIGN (Text Left / Image Right) {%- endcomment %}
    <div class="tno-manifesto__section" id="design">
      <div class="tno-manifesto__grid grid grid--1-col grid--2-col-desktop grid--2-col-tablet">
        <div class="tno-manifesto__content grid__item">
          <h2 class="tno-manifesto__title">{{ design_title }}</h2>
          {%- if design_text != blank -%}
            <div class="tno-manifesto__text">
              {{ design_text }}
            </div>
          {%- endif -%}
        </div>
        <div class="tno-manifesto__media grid__item">
          {%- if design_image != blank -%}
            {{
              design_image
              | image_url: width: 1200
              | image_tag:
                loading: 'lazy',
                sizes: '(min-width: 990px) 50vw, 100vw',
                alt: design_image.alt,
                class: 'tno-manifesto__image'
            }}
          {%- else -%}
            <div class="tno-manifesto__placeholder">
              {{ 'lifestyle-3' | placeholder_svg_tag: 'placeholder-svg' }}
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>

    {%- comment %} Section 4: ETHICS (Image Left / Text Right) {%- endcomment %}
    <div class="tno-manifesto__section" id="ethics">
      <div class="tno-manifesto__grid grid grid--1-col grid--2-col-desktop grid--2-col-tablet tno-manifesto__grid--reverse">
        <div class="tno-manifesto__media grid__item">
          {%- if ethics_image != blank -%}
            {{
              ethics_image
              | image_url: width: 1200
              | image_tag:
                loading: 'lazy',
                sizes: '(min-width: 990px) 50vw, 100vw',
                alt: ethics_image.alt,
                class: 'tno-manifesto__image'
            }}
          {%- else -%}
            <div class="tno-manifesto__placeholder">
              {{ 'lifestyle-4' | placeholder_svg_tag: 'placeholder-svg' }}
            </div>
          {%- endif -%}
        </div>
        <div class="tno-manifesto__content grid__item">
          <h2 class="tno-manifesto__title">{{ ethics_title }}</h2>
          {%- if ethics_text != blank -%}
            <div class="tno-manifesto__text">
              {{ ethics_text }}
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Manifesto",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Section 1: Origin of the Glitch"
    },
    {
      "type": "text",
      "id": "origin_title",
      "label": "Origin Title",
      "default": "ORIGIN OF THE GLITCH"
    },
    {
      "type": "richtext",
      "id": "origin_text",
      "label": "Origin Text",
      "default": "<p>In the noise of digital signals, we found a glitch. Not a bug, but a feature—a moment where the simulation reveals itself. TRUST.NO.OUTPUT was born from questioning what we're told to consume, to wear, to become.</p>"
    },
    {
      "type": "image_picker",
      "id": "origin_image",
      "label": "Origin Image"
    },
    {
      "type": "header",
      "content": "Section 2: Rules of the Simulation"
    },
    {
      "type": "text",
      "id": "philosophy_title",
      "label": "Philosophy Title",
      "default": "RULES OF THE SIMULATION"
    },
    {
      "type": "richtext",
      "id": "philosophy_text",
      "label": "Philosophy Text",
      "default": "<p>We don't break the rules—we rewrite them. Every piece we create is a statement against predefined outputs. Mode becomes our interface for questioning reality.</p>"
    },
    {
      "type": "image_picker",
      "id": "philosophy_image",
      "label": "Philosophy Image"
    },
    {
      "type": "header",
      "content": "Section 3: Design DNA"
    },
    {
      "type": "text",
      "id": "design_title",
      "label": "Design Title",
      "default": "DESIGN DNA"
    },
    {
      "type": "richtext",
      "id": "design_text",
      "label": "Design Text",
      "default": "<p>Minimalism meets disruption. Clean lines cut through digital noise. Each garment is encoded with intention—a conscious choice in a world of automated consumption.</p>"
    },
    {
      "type": "image_picker",
      "id": "design_image",
      "label": "Design Image"
    },
    {
      "type": "header",
      "content": "Section 4: Human Layer"
    },
    {
      "type": "text",
      "id": "ethics_title",
      "label": "Ethics Title",
      "default": "HUMAN LAYER"
    },
    {
      "type": "richtext",
      "id": "ethics_text",
      "label": "Ethics Text",
      "default": "<p>Behind every design is a human. We prioritize ethical production, conscious materials, and transparent processes. Fashion shouldn't cost the planet—or people.</p>"
    },
    {
      "type": "image_picker",
      "id": "ethics_image",
      "label": "Ethics Image"
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 60
    }
  ],
  "presets": [
    {
      "name": "Manifesto"
    }
  ]
}
{% endschema %}
