{% comment %}
  TRUST.NO.OUTPUT — Manifesto Text Section
  
  Custom text section with optimized typography for philosophy/manifesto content:
  - Max-width 55-65ch for optimal readability
  - Line-height 1.6-1.7
  - Lighter gray text color for better contrast
{% endcomment %}

<style>
  .manifesto-text-tno {
    padding: var(--space-xl, 4rem) var(--space-md, 2rem);
    background: var(--color-deep-black, #0a0a0a);
    display: flex;
    justify-content: center;
  }

  .manifesto-text-tno__container {
    max-width: 65ch;
    width: 100%;
  }

  .manifesto-text-tno__content {
    font-family: var(--font-clean, system-ui, -apple-system, sans-serif);
    font-size: clamp(1rem, 2.5vw, 1.125rem);
    font-weight: 300;
    line-height: 1.65;
    color: #c8c8c8; /* Lighter gray for better readability */
    letter-spacing: 0.01em;
  }

  .manifesto-text-tno__content p {
    margin: 0 0 1.5em;
  }

  .manifesto-text-tno__content p:last-child {
    margin-bottom: 0;
  }

  .manifesto-text-tno__content strong,
  .manifesto-text-tno__content b {
    font-weight: 500;
    color: #e0e0e0;
  }

  .manifesto-text-tno__content em,
  .manifesto-text-tno__content i {
    font-style: italic;
    color: var(--color-cyan, #00ffff);
  }

  /* Mobile optimization */
  @media (max-width: 768px) {
    .manifesto-text-tno {
      padding: var(--space-lg, 3rem) var(--space-sm, 1.5rem);
    }

    .manifesto-text-tno__container {
      max-width: 55ch;
    }

    .manifesto-text-tno__content {
      line-height: 1.7;
    }
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .manifesto-text-tno * {
      animation: none !important;
      transition: none !important;
    }
  }
</style>

{%- liquid
  assign content = section.settings.content
-%}

<section class="manifesto-text-tno">
  <div class="manifesto-text-tno__container">
    <div class="manifesto-text-tno__content">
      {{ content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Manifesto Text (TNO)",
  "class": "section-manifesto-text-tno",
  "settings": [
    {
      "type": "richtext",
      "id": "content",
      "label": "Content",
      "default": "<p>Add your manifesto text here...</p>"
    }
  ],
  "presets": [
    {
      "name": "Manifesto Text (TNO)",
      "category": "Trust.No.Output"
    }
  ]
}
{% endschema %}
