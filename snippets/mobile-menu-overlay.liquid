
<div
  id="mobile-menu"
  class="tno-mobile-menu-overlay"
  aria-hidden="true"
>
  <button
    type="button"
    class="tno-mobile-menu-close"
    data-tno="close-mobile-menu"
    aria-label="Close menu"
  ></button>

  {%- comment -%} Hier bleibt der eigentliche Menü-Inhalt wie im Drawer {%- endcomment -%}
  <div class="tno-mobile-menu-inner">
    {{ 'tno-bottom-tabs' | render }}
  </div>
  <!-- Fallback: transparent hit area below overlay to allow MENU button clicks -->
  <div class="tno-mobile-menu-hitbox"></div>
</div>
